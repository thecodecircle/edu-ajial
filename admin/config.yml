
backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: '_site/assets/uploads'
collections:
  - name: 'peula'
    label: 'Peula'
    folder: '_site/_posts/'
    fields:
      - { name: Title }
